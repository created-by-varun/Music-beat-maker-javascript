window.addEventListener('load', () => {
    const sounds = document.querySelectorAll(".sound");
    const pads = document.querySelectorAll.querySelectorAll(".pads div");


    //sound
    pads.foreach(pad => {
        pad.addEventListener('click', function(){
            
        });
    });
});